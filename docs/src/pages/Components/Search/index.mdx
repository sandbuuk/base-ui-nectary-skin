import { Code } from '~/components/Code'
import { Example } from '~/components/Example'
import { CompositionExample } from './examples/Composition.tsx?example'

# Search

Search implementation is a pure composition of Input and Action Menu components.

Precomposition as some kind of a blackbox-like molecule provided from our side would require to expose dozens of *namespaced* props and events, with at least 3 different types of `aria-label`.

## Table of contents

## Examples

### Composition

<Example Component={CompositionExample}/>
