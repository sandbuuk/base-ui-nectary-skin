import { Code } from '~/components/Code'
import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { PlaceholderExample } from './examples/Placeholder.tsx?example'
import {
  ReactPropsTable,
  ReactHandlersTable,
  ElementPropertiesTable,
  ElementAttributesTable,
  ElementEventsTable
} from '@sinch-engage/nectary/select-button/types.ts?api'

# Select Button

Select Button is a special building block for high-level compositions like [Select](?path=/components/select).

It looks like Input but functions like Button.

## Table of contents

## Examples

### Simple

<Example Component={SimpleExample}/>

### Placeholder

Placeholder is shown when required `text` attribute is an empty string, just like with empty `value` in Input.

<Example Component={PlaceholderExample}/>

## API

#### Slots

##### icon

`icon` slot puts an icon to the left side.

#### React

##### Props

<ReactPropsTable/>

##### Handlers

<ReactHandlersTable/>

#### Element

##### Properties

<ElementPropertiesTable/>

##### Attributes

<ElementAttributesTable/>

##### Events

<ElementEventsTable/>
