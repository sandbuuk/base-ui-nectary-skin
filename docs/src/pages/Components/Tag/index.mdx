import { Code } from '~/components/Code'
import { Example } from '~/components/Example'
import { ColorsExample } from './examples/Colors.tsx?example'
import { IconExample } from './examples/Icon.tsx?example'
import { SmallExample } from './examples/Small.tsx?example'
import {
  ReactPropsTable,
  ElementPropertiesTable,
  ElementAttributesTable,
} from '@sinch-engage/nectary/tag/types.ts?api'

# Tag

Just a tag.

Works with the same `color` value as [Color Menu](?path=/components/colormenu).

## Table of contents

## Examples

### Colors

Nectary ships different colors to be used in tags. Color is just a string passed to a `color` attribute

<Example Component={ColorsExample}/>

### Icon

Tags can have any icon inserted into `icon` slot

<Example Component={IconExample}/>

### Small

Tag can be made smaller

<Example Component={SmallExample}/>

## API

### Slots

#### icon

`icon` slot puts an icon to the left side.

### React

#### Props

<ReactPropsTable/>

### Element

#### Properties

<ElementPropertiesTable/>

#### Attributes

<ElementAttributesTable/>
