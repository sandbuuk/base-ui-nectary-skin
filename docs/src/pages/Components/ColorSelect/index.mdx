import { Code } from '~/components/Code'
import { Example } from '~/components/Example'
import { IconButtonExample } from './examples/IconButton.tsx?example'
import { SelectButtonExample } from './examples/SelectButton.tsx?example'

# Color Select

Color Select is a composition of a target component, [Popover](?path=/components/popover) and [Color Menu](?path=/components/colormenu).

There likely will be Color Picker component too.

## Table of contents

## Examples

### Icon Button

Use down/up/right/left arrow keys to navigate and space/enter to select with keyboard.

<Example Component={IconButtonExample}/>

### Select Button

<Example Component={SelectButtonExample}/>
