import { Code } from '~/components/Code'
import { Example } from '~/components/Example'
import { ButtonExample } from './examples/Button.tsx?example'
import { MultipleExample } from './examples/Multiple.tsx?example'

# Select

Select is a composition of a target component, Popover and Select Menu.

## Table of contents

## Examples

### Button

Use down/up arrow keys to navigate and space/enter to select with keyboard.

<Example Component={ButtonExample}/>

### Multiple

We currently don't have a well-defined UX/UI pattern of how to show multiple selection.

<Example Component={MultipleExample}/>
