import { Example } from '~/components/Example'
import { FilePickerExample } from './examples/FilePicker.tsx?example'
import { FileDropExample } from './examples/FileDrop.tsx?example'

# Examples

File Input is a composition of [FilePicker](?path=/components/filepicker/examples) or [FileDrop](?path=/components/filedrop/examples) + [Button](?path=/components/button/examples) and additional [FileStatus](?path=/components/filestatus/examples) components for upload progress feedback.

## File Picker

Choose at least 3 files and the example below will emulate the actual uploading functionality with all file status types.

<Example Component={FilePickerExample}/>

## File Drop

Choose at least 3 files and the example below will emulate the actual uploading functionality with all file status types.

<Example Component={FileDropExample}/>
