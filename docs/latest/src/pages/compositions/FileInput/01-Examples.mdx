import { Example } from '~/components/Example'
import { FileInputExample } from './examples/FileInput.tsx?example'

# Examples

File Input is a composition of [sinch-file-picker](?path=/components/filepicker/examples), [sinch-button](?path=/components/button/examples) and [sinch-file-status](?path=/components/filestatus/examples)

## Composition

Choose at least 3 files and the example below will emulate the actual uploading functionality with all file status types.

<Example Component={FileInputExample}/>
