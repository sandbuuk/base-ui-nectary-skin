import { Example } from '~/components/Example'
import { CodeExample } from './examples/Code.tsx?example'
import { SizeExample } from './examples/Size.tsx?example'

# Examples

`CodeTag` component renders value provided to `text` prop with Mono font and background

## Code

<Example Component={CodeExample}/>

## Size

The size of the font is inherited from the parent `Text` element

<Example Component={SizeExample}/>
