import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'

# Examples

Spinner components are used to indicate that a webpage or application is loading, or that a request for data is being processed.

## Sizes

The spinner comes in three different sizes.

<Example Component={SimpleExample}/>
