import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { EmptyExample } from './examples/Empty.tsx?example'

# Examples

Field provides a consistent wrapper for form inputs with support for labels, optional text, tooltips, additional helper text, and error messages.

## Simple

A field with all available options: label, optional text, tooltip, additional text, and invalid text.

<Example Component={SimpleExample}/>

## Empty top section attributes

A field without label and optional text, showing only the input with additional and invalid text.

<Example Component={EmptyExample}/>
