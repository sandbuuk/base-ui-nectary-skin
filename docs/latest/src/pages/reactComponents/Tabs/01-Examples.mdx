import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { IconOnlyExample } from './examples/IconOnly.tsx?example'

# Examples

Tabs are a navigation component that allows to separate content into containers that are shown one at a time. They can be used to organize and present large amounts of information or content in a way that is easy for users to understand and navigate. The labels in the tabs should work in a way that the user can recognize what is in each container.

## Simple

<Example Component={SimpleExample}/>

## Icon only

Tab components that contain only icons are often used when space is limited or when a minimalist design is desired. They can be a useful way to provide navigation and access to different sections or views within a webpage or application without taking up a lot of space.

<Example Component={IconOnlyExample}/>
