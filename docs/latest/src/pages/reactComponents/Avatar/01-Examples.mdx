import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { SilhouetteExample } from './examples/Silhouette.tsx?example'
import { SizeExample } from './examples/Size.tsx?example'
import { StatusExample } from './examples/Status.tsx?example'
import { BadgeExample } from './examples/Badge.tsx?example'
import { ColorExample } from './examples/Color.tsx?example'

# Examples

Avatar is used to represent a user entity. It supports images or letters.

## Simple

<Example Component={SimpleExample}/>

## Silhouette

Avatar displays silhouette when `src` and `alt` props are omitted

<Example Component={SilhouetteExample}/>

## Size

Avatars comes in three different sizes. Adapt the size so it can fit the UI accordinginly

<Example Component={SizeExample}/>

## Status

Status can be indicated in the avatar with the status property

<Example Component={StatusExample}/>

## Badge

A badge in an avatar is a small visual indicator that is displayed next to the avatar to provide additional information about the user or the context in which the avatar is being used. It can contain numbers or not.

<Example Component={BadgeExample}/>

## Color

Avatars that do not have image, are represented with text. Usually, the text inside are the initials of name. These default avatars, comes in different colors that can help creating relation and different between the avatars in the interface.

<Example Component={ColorExample}/>
