import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'

# Examples

Grid is a responsive grid-based layout system, with rows and columns.

## Simple

In the below example we specify a grid.
Items can have different widths (columns) depending on the size of the screen (`xl, l, m, s`).
In the **example below** the first `<sinch-grid-item>` has a width of 3 columns for `xl` screens, while for other screen sizes it has
4 columns in width.

<Example Component={SimpleExample}/>
