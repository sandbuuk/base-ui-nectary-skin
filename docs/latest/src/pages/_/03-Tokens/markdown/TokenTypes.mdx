import { SegmentedMarkdownTabs } from '../components';
import { ColorTypeIllustration } from '../illustrations';

<SegmentedMarkdownTabs>
  * Color
  ### Color
  1. These tokens define the colors values used in the design system.
  These color tokens allow designers to easily apply consistent colors to different components and interfaces,
  ensuring visual harmony and brand consistency in the user interface.
  2. <ColorTypeIllustration />
  * Typography
  ### Typography
  1. Typography Tokens: Typography tokens define the typography styles used in the design system,
  including font families, font sizes, line heights, and font weights.
  These tokens provide a consistent typographic hierarchy across different components and interfaces,
  ensuring readability and visual consistency in the design system.
  * Shape Tokens
  ### Shape Tokens
  1. Shape tokens define the shapes and radius used in various components, such as buttons, cards, and inputs.
  These tokens ensure consistency in the rounded corners, borders, and shapes of different components, creating a cohesive visual language in the design system.
  * Elevation Tokens
  ### Elevation Tokens
  1. Elevation tokens define the depth and shadows used in components to create a sense of depth and hierarchy. These tokens ensure consistency in the shadows and elevations used across different components, providing a unified visual hierarchy and depth perception in the design system.
  * Size Tokens
  ### Size Tokens
  1. Size tokens define the spacing, margins, and paddings used in components and layouts. These tokens ensure consistency in the spacing and alignments across different components, ensuring a harmonious and visually pleasing layout in the design system.
</SegmentedMarkdownTabs>
