import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { PlaceholderExample } from './examples/Placeholder.tsx?example'

# Select Button

Select Button is a special building block for high-level compositions like [Select](?path=/components/select/examples).

It looks like Input but functions like Button.

## Examples

### Simple

<Example Component={SimpleExample}/>

### Placeholder

Placeholder is shown when required `text` attribute is an empty string, just like with empty `value` in Input.

<Example Component={PlaceholderExample}/>
