import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { IconExample } from './examples/Icon.tsx?example'
import { ButtonExample } from './examples/Button.tsx?example'
import { LinkExample } from './examples/Link.tsx?example'
import { IllustrationExample } from './examples/Illustration.tsx?example'
import { DraggableExample } from './examples/Draggable.tsx?example'

# Examples

Cards are containers that group related information. They help to organize and divide content making it easier to scan big chunks of information.

## Simple

A simple card in Nectary is a user interface element that is used to present information or content in a compact, organized manner. It typically consists of a container with a fixed or variable size, and includes only a title and text.They can be used in a variety of contexts, such as listing products or services, showcasing articles or blog posts, or displaying information about a particular topic.

The title and text of the simple card can be customized to fit the needs of the design and the context in which it is being used.

<Example Component={SimpleExample}/>

## Icon

The icon can be used to add visual interest or context to the card. The design and layout of the card is kept minimal and straightforward, with a focus on presenting information in a clear and concise manner.

<Example Component={IconExample}/>

## Button

 The button allows users to take a specific action, such as viewing more details or accessing related content.

<Example Component={ButtonExample}/>

## Link

The link allows users to navigate to a specific web page or location within the application

<Example Component={LinkExample}/>

## Illustration

The illustration is a visual element that can be used to add context, interest, or personality to the card. It can be an image, icon, or other graphical element that is relevant to the content of the card. The illustration can help users quickly understand the content of the card and engage with it more effectively.

<Example Component={IllustrationExample}/>

## Draggable

Sinch Card implements HTML5 drag and drop API. Set `draggable` attribute to enable dragging by card title.

<Example Component={DraggableExample}/>
