import { Example } from '~/components/Example'
import { MarkdownExample } from './examples/Markdown.tsx?example'
import { SizeExample } from './examples/Size.tsx?example'

# Examples

`sinch-rich-text` component parses and renders markdown subset, passed to a `text` attribute.

## Markdown

<Example Component={MarkdownExample}/>

## Size

Rich Text can have different font sizes

<Example Component={SizeExample}/>
