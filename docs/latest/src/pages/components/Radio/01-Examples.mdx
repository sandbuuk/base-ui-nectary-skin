import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { DisabledExample } from './examples/Disabled.tsx?example'
import { FieldExample } from './examples/Field.tsx?example'
import { DirectionExample } from './examples/Direction.tsx?example'
import { CustomExample } from './examples/Custom.tsx?example'

# Examples

## Simple

Simple example

<Example Component={SimpleExample}/>

## Disabled

Radio option can be disabled

<Example Component={DisabledExample}/>

## Field

Radio group is a form input element, so can be wrapped in a [sinch-field](?path=/components/field/examples)

<Example Component={FieldExample}/>

## Direction and Gap

There's an option to change direction of the radio buttons and to customize the gap between them.

<Example Component={DirectionExample}/>


## Custom

You can do a custom composition if you want to customize the text like adding an icon.

<Example Component={CustomExample}/>