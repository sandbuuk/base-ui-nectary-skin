import { Example } from '~/components/Example'
import { SingleIconExample } from './examples/SingleIcon.tsx?example'
import { SizesExample } from './examples/Sizes.tsx?example'
import { RadiusExample } from './examples/Radius.tsx?example'
import { CustomColorsExample } from './examples/CustomColors.tsx?example'
import { AllIconsExample } from './examples/AllIcons.tsx?example'

> **⚠️ DEPRECATION ATTRIBUTE CANDIDAT** <br />
> Because the component now exposes a dedicated border-radius token, there’s no longer a need to differentiate between `square` and `round` icon variants. These variants are deprecated and will be removed in future versions of Nectary.

# Examples

## Default icon

<Example Component={SingleIconExample}/>

## Custom size

<Example Component={SizesExample}/>

## Custom radius

You can presizely adjust the border radius property by overwriting the `--sinch-comp-icon-channel-shape-radius` variable.

<Example Component={RadiusExample}/>

## Custom colors

You can adjust the icon's colors. Background can apply either a flat color or gradient. Icon shape's can only apply flat colors.

<Example Component={CustomColorsExample}/>

## All icons

<Example Component={AllIconsExample} shouldHideCode/>
