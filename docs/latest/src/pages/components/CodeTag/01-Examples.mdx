import { Example } from '~/components/Example'
import { CodeExample } from './examples/Code.tsx?example'
import { SizeExample } from './examples/Size.tsx?example'

# Examples

`sinch-code-tag` component renders value provided to `text` attribute with Mono font and background

## Code

<Example Component={CodeExample}/>

## Size

The size of the font is inherited from the parent `sinch-text` element

<Example Component={SizeExample}/>
