import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { BottomExample } from './examples/Bottom.tsx?example'
import { BottomComplexExample } from './examples/BottomComplex.tsx?example'
import { FieldExample } from './examples/Field.tsx?example'
import { RowsExample } from './examples/Rows.tsx?example'
import { MinRowsExample } from './examples/MinRows.tsx?example'
import { ResizableExample } from './examples/Resizable.tsx?example'
import { DisabledExample } from './examples/Disabled.tsx?example'

# Examples

Textarea is a multiline text field.

It's a stateless and [controlled](https://reactjs.org/docs/forms.html#controlled-components) component.

## Simple

<Example Component={SimpleExample}/>

## Field

Textarea wrapped in `sinch-field` component

<Example Component={FieldExample}/>

## Resizable

Shows the resize handle to manually resize the height of textarea
> User-resizable Textarea does not self-resize on typing

<Example Component={ResizableExample}/>

## Rows

Initial Teaxarea height defined as number of text lines

<Example Component={RowsExample}/>

## Min rows

Used with `resizable` attribute, to prevent shrinking Textarea height below certain number of text lines.

In this example `minRows` value is set to `3`, so you cannot shrink the height of the Textarea below 3 lines.

<Example Component={MinRowsExample}/>

## Disabled

<Example Component={DisabledExample}/>

## Bottom Toolbar

Textarea provides slot named `bottom` to allow placing different components on a toolbar

<Example Component={BottomExample}/>

## Toolbar Popovers

<Example Component={BottomComplexExample}/>
