import { Example } from '~/components/Example'
import { SimpleExample } from './examples/Simple.tsx?example'
import { MultipleExample } from './examples/Multiple.tsx?example'
import { AcceptExample } from './examples/Accept.tsx?example'
import { InvalidExample } from './examples/Invalid.tsx?example'
import { CompositionExample } from './examples/Composition.tsx?example'

# Examples

File Drop encapsulates file drag and drop area and wraps a button with File Picker.

## Simple

File Drop provides an array of [files](https://developer.mozilla.org/en-US/docs/Web/API/File) to upload.

<Example Component={SimpleExample}/>

## Multiple

Allows to choose multiple files.

<Example Component={MultipleExample}/>

## Accept

[A unique file type specifier](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file#unique_file_type_specifiers) that describes a type of file that may be selected by the user:

* a valid case-insensitive filename extension, starting with a period (".") character. For example: `.jpg`, `.pdf`, or `.doc`.
* a valid MIME type string, with no extensions
* string `audio/*` meaning "any audio file"
* string `video/*` meaning "any video file"
* string `image/*` meaning "any image file"

>The accept attribute takes a string containing one or more of these unique file type specifiers as its value, separated by commas.

<Example Component={AcceptExample}/>

## Invalid

Unlike with the File Picker button it's possible to drag and drop arbitraty files (and even selected text) onto the droppable area. There is a way to validate this and show an error message via `-invalid` event and mix of `invalid` and `invalidtext` attributes.

Possible invalid things:

* `size` – dropped file is too large
* `multiple` – there must be only a single file dropped
* `accept` – dropped file doesn't satisfy mime type or extension limitations

Try to drag and drop a non-`.jpg` or larger than 1KB file:

<Example Component={InvalidExample}/>

## Composition

Choose at least 3 files and the example below will emulate the actual uploading functionality with all file status types.

<Example Component={CompositionExample}/>
