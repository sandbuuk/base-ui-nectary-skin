import { Example } from 'docs-common'
import { RefExample } from './examples/Ref.tsx?example'
import { SysExample } from './examples/Sys.tsx?example'

## Sys Tokens

Be aware that the "Value" column is for the Base Light theme and depending on the theme you have selected, the example might not correspond.

<Example Component={SysExample}/>

## Ref Tokens

Be aware that the "Value" column is for the Base Light theme and depending on the theme you have selected, the example might not correspond.

<Example Component={RefExample}/>

