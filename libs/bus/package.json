{
  "name": "@sinch-engage/bus",
  "version": "1.0.0",
  "main": "src/index.ts",
  "files": [
    "lib/"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "prebuild": "rimraf lib/",
    "build": "NODE_ENV=production babel src/ --extensions '.ts' --out-dir lib/",
    "postbuild": "tsc --outDir lib/"
  }
}
