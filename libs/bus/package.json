{
  "name": "@saas/bus",
  "version": "1.0.0",
  "main": "src/index.ts",
  "files": [
    "lib/"
  ],
  "publishConfig": {
    "registry": "https://nexus.int.clxnetworks.net/repository/sinch_npm/"
  },
  "scripts": {
    "prebuild": "rimraf lib/",
    "build": "NODE_ENV=production babel src/ --extensions '.ts' --out-dir lib/",
    "postbuild": "tsc --outDir lib/"
  }
}
