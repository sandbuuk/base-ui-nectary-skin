{
  "name": "@saas/components",
  "version": "0.0.0",
  "files": [
    "theme.css",
    "*/index.js",
    "*/index.d.ts"
  ],
  "scripts": {
    "prebuild": "rimraf utils.{js,d.ts} */index.{js,d.ts}",
    "build": "babel . --extensions '.ts' --ignore '*.d.ts' --out-dir .",
    "postbuild": "tsc"
  },
  "devDependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.5",
    "html-minifier-terser": "^6.0.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.2"
  }
}
